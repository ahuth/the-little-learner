{
  "name": "the-little-learner",
  "private": true,
  "scripts": {
    "test": "jest",
    "types": "tsc --noEmit",
    "validate": "npm run types && npm run test"
  },
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "jest": "^29.4.3",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.5"
  }
}
